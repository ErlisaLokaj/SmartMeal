# Copy this file to .env and edit as needed for local development
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=smartmeal
# Connection string used by the app (if you prefer to set it directly)
# POSTGRES_DB_URL=postgresql+psycopg2://postgres:postgres@db:5432/smartmeal
